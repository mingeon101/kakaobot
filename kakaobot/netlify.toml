[build]
  # 필요한 파이썬 패키지를 설치합니다.
  command = "pip install -r requirements.txt"
  
  # Netlify Functions가 위치하는 디렉토리를 지정합니다.
  functions = "functions"
  
[functions]
  # 파이썬 런타임 버전을 지정합니다.
  python_runtime = "python3.9"
  # (또는 현재 Netlify가 지원하는 다른 버전)